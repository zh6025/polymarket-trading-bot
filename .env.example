# Runtime settings / 运行配置
# true = simulate only, false = place real orders
DRY_RUN=true
# Default order size submitted by the bot
ORDER_SIZE=5
# Polling interval in seconds, minimum 1
CHECK_INTERVAL_SEC=5
