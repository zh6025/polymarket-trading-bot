export class ApiError extends Error {
    status;
    data;
    constructor(message, status, data) {
        super(message);
        this.name = "ApiError";
        this.status = status;
        this.data = data;
    }
}
export const L1_AUTH_UNAVAILABLE_ERROR = new Error("Signer is needed to interact with this endpoint!");
export const L2_AUTH_NOT_AVAILABLE = new Error("API Credentials are needed to interact with this endpoint!");
export const BUILDER_AUTH_NOT_AVAILABLE = new Error("Builder API Credentials needed to interact with this endpoint!");
export const BUILDER_AUTH_FAILED = new Error("Builder key auth failed!");
//# sourceMappingURL=errors.js.map