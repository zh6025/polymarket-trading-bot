import { createHmac } from "node:crypto";
function replaceAll(s, search, replace) {
    return s.split(search).join(replace);
}
/**
 * Builds an hmac signature
 * @param signer
 * @param key
 * @param secret
 * @param passphrase
 * @returns string
 */
export const buildHmacSignature = (secret, timestamp, method, requestPath, body) => {
    let message = timestamp + method + requestPath;
    if (body !== undefined) {
        message += body;
    }
    const base64Secret = Buffer.from(secret, "base64");
    const hmac = createHmac("sha256", base64Secret);
    const sig = hmac.update(message).digest("base64");
    // NOTE: Must be url safe base64 encoding, but keep base64 "=" suffix
    // Convert '+' to '-'
    // Convert '/' to '_'
    const sigUrlSafe = replaceAll(replaceAll(sig, "+", "-"), "/", "_");
    return sigUrlSafe;
};
//# sourceMappingURL=hmac.js.map