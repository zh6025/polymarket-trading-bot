version: '3.8'

services:
  polymarket-bot:
    build: ..
    container_name: polymarket-bot
    restart: unless-stopped
    env_file:
      - .env.production
    volumes:
      - ../logs:/app/logs
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "5"
